# Dead code audit — 2026-05-07

Scope: `apps/medconcierge/src/lib/`, `apps/web/lib/`, `packages/ai/`,
`packages/notifications/`. Cross-checked against `apps/`, `packages/`, `scripts/`.

## Definitely dead (0 references outside the file/dir itself)

| File | Export | LOC | Recommendation |
|------|--------|-----|----------------|
| `apps/medconcierge/src/lib/ai-with-functions.ts` | `runWhatsAppReplyWithFunctions` (whole file) | 276 | Delete file. Superseded by `packages/ai/run-with-tools.ts:runWhatsAppReplyWithTools`. |
| `apps/medconcierge/src/lib/whatsapp-functions.ts` | `WHATSAPP_TOOLS`, `handleCheckAvailability`, `handleBookAppointment`, `handleCancelAppointment`, `handleGetPatientAppointments`, `dispatchFunctionCall` (whole file) | 404 | Delete file. Replaced by `packages/ai/tools.ts` + `packages/ai/tool-executors.ts`. |
| `apps/medconcierge/src/lib/notification-helpers.ts` | `sendAppointmentNotification`, `createDoctorNotification` | 58 | Delete file. Both helpers unused; live path is `notify-doctor.ts` + `notifications.ts`. |
| `apps/web/lib/tenant-cache.ts` | `getCachedTenant`, `setCachedTenant`, `invalidateTenantCache` | 22 | Delete file. No imports anywhere. |
| `apps/web/lib/image-validation.ts` | `validateImage`, `validateImageBuffer`, `ImageValidationResult` | 56 | Delete file. No imports anywhere. |
| `apps/web/lib/pdf-signature.ts` | `generatePdfSignature` | 20 | Delete file. The live copy is duplicated locally inside `apps/web/app/api/quotes/[quoteId]/pdf/route.ts:22`. |
| `apps/web/lib/theme.ts` | `Theme`, `ThemeContext`, `useTheme` | 17 | Delete file. Only `apps/mobile/lib/theme.ts` is consumed (different module). |
| `apps/web/lib/cn.ts` | `cn` | 6 | Delete file. No consumer in apps/web; mobile/medconcierge use their own. |
| `apps/web/lib/mock-data.ts` | `MOCK_QUOTES`, `getMockQuotes` | 143 (file partial) | Drop the two quote exports; `MOCK_TENANT`, `MOCK_PRODUCTS`, `getMockTenant`, `getMockProducts` are still used by `app/[tenant]/page.tsx`. |
| `packages/ai/src/` (entire directory) | `chat.ts`, `client.ts`, `embeddings.ts`, `index.ts`, `knowledge.ts`, `settings.ts`, `types.ts` | 532 | Delete directory. `package.json` `main` points to `./index.ts` (top level); `src/` is an unfinished refactor leftover, no external import resolves through it. |
| `packages/notifications/whatsapp.ts` | `verifyWebhook` | ~10 | Drop the export and its re-export in `packages/notifications/index.ts`. Webhook verification is performed inline in `apps/web/app/api/webhooks/whatsapp/route.ts`. |
| `packages/notifications/index.ts` | re-export `WebPushSubscriptionLike`, `WebPushResult` | — | Drop from barrel; both types are only used inside `web-push.ts`. |

Total roughly **1,544 LOC** strictly orphaned.

## Suspect (used internally only — could be unexported, not removed)

| File | Export | Notes |
|------|--------|-------|
| `apps/medconcierge/src/lib/bot-messages.ts:renderBotMessage` | only called by `formatBotMessage` in same file | downgrade to non-exported. |
| `apps/medconcierge/src/lib/api-auth.ts:hashApiKey`, `ApiAuthContext` | only consumed inside `api-auth.ts` | unexport. |
| `apps/medconcierge/src/lib/patient-comms.ts:TrackCommArgs` | only used internally | unexport. |
| `apps/medconcierge/src/lib/lead-autocontact.ts:AutoContactOptions`, `AutoContactResult` | only internal | unexport. |
| `apps/medconcierge/src/lib/notify-doctor.ts:DoctorPushPayload` | only internal | unexport. |
| `packages/ai/budget.ts:BudgetCheckResult` | re-exported from `packages/ai/index.ts:381` but no external consumer | drop re-export. |
| `packages/ai/tool-executors.ts:getDoctorContext` | declared, never read; only `setDoctorContext` is consumed (run-with-tools.ts) | delete getter. |
| `apps/medconcierge/src/lib/sidebar-items.ts:DEFAULT_VISIBLE_IDS` | unused | delete. |
| `packages/ai/prompts.ts:getTenantVertical` | re-exported, no consumer | drop. |
| `packages/ai/specialty-templates.ts:getSpecialtyList` | re-exported once via `index.ts`, no further consumer | drop. |

## Migrating-from leftovers (used to be needed, no longer)

- `apps/medconcierge/src/lib/safe-cookie-get.ts:safeGetAuthCookie` — header
  comment documents the migration to `@supabase/ssr@0.10`. Wrapper now just
  returns its input. Still imported in 3 places:
    - `apps/medconcierge/src/middleware.ts` (active call)
    - `apps/medconcierge/src/app/api/auth/magic-link/route.ts` (active call)
    - `apps/medconcierge/src/lib/auth.ts:3` — **dead import** (the file no
      longer references the symbol; `getAll/setAll` cookie API was adopted).
  Recommendation: inline `value || undefined` at the two real call sites and
  delete `safe-cookie-get.ts`. Then remove the dead `auth.ts` import.
- `apps/medconcierge/src/app/api/dashboard/settings/messages/route.ts` and
  `bot/route.ts` only depend on `auth.ts` via `getAuthTenant`. They do **not**
  reach `safeGetAuthCookie` at runtime — the unused import in `auth.ts` is
  the only remaining link. Removing the import is safe.
- `packages/ai/src/` — the whole subdir is a partially-completed split of
  `packages/ai/index.ts`. Workspace `main` still points to the monolithic
  top-level file; no path resolves into `src/`.

## Recommended deletions

1. `apps/medconcierge/src/lib/ai-with-functions.ts`
2. `apps/medconcierge/src/lib/whatsapp-functions.ts`
3. `apps/medconcierge/src/lib/notification-helpers.ts`
4. `apps/medconcierge/src/lib/safe-cookie-get.ts` (after inlining at 2 sites)
5. `apps/web/lib/tenant-cache.ts`
6. `apps/web/lib/image-validation.ts`
7. `apps/web/lib/pdf-signature.ts`
8. `apps/web/lib/theme.ts`
9. `apps/web/lib/cn.ts`
10. `packages/ai/src/` (entire directory: 7 files, 532 LOC)
11. Trim `apps/web/lib/mock-data.ts:MOCK_QUOTES` + `getMockQuotes`.
12. Trim `packages/notifications/whatsapp.ts:verifyWebhook` and its barrel re-export.
13. Drop the unused `auth.ts:3` import of `safeGetAuthCookie` (item 4 will force this).

After these, run `pnpm build` (medconcierge + web) to confirm the tree still
typechecks. None of the targeted symbols have live call sites, so the build
is expected to remain green.
